import React from 'react'
import Layout from '../components/layout'

export default props => (
  <Layout location={props.location}>
    <div>
      <h1>these are the services I offer</h1>
    </div>
  </Layout>
)
